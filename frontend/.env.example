# Optional: Override backend URL for production or custom deployment
# If left blank, defaults to local dev server (http://localhost:4000) or Render production URL
# VITE_BACKEND_URL=https://your-ec2-domain-or-ip.com

VITE_GOOGLE_CLIENT_ID=your_google_client_id_here
